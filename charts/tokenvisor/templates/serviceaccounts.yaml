apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.emu.serviceAccountName }}
  namespace: {{ include "tokenvisor.namespace" . }}
  labels:
    {{- include "tokenvisor.labels" . | nindent 4 }}
    app: emu
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.studio.serviceAccountName }}
  namespace: {{ include "tokenvisor.namespace" . }}
  labels:
    {{- include "tokenvisor.labels" . | nindent 4 }}
    app: studio
