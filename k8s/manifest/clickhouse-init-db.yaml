apiVersion: v1
kind: "ConfigMap"
metadata:
  name: "init-clickhouse-db"
data:
  01_create_databases.sh: |
    #!/bin/bash

    # Environment variables
    CH_USER=${CH_USER:-default}  # Default to 'default' if CH_USER is not set
    CH_PASSWORD=${CH_PASSWORD:-} # Default to empty password if CH_PASSWORD is not set
    CH_HOST=${CH_HOST:-clickhouse-tokenvisor-clickhouse.svc.cluster.local} # Default to service name
    EXPECTED_CLUSTER_COUNT=${EXPECTED_CLUSTER_NUMBER:-3} # Default to 3 replicas if not set
    CLUSTER_NAME=${CLUSTER_NAME:-tv-cluster} # Default to 'tv-cluster' if not set
    READONLY_USER=${READONLY_USER:-defaultreadonly} # Default to 'default' if READONLY_USER is not set
    READONLY_PASSWORD=${READONLY_PASSWORD:-} # Default to empty password if READONLY_PASSWORD is not set

    # Function to verify table is created.
    create_table_with_retry() {
      local table_name=$1
      local create_query=$2
      local max_retries=5
      local retry_delay=10
      local retries=0

      while [ $retries -lt $max_retries ]; do
        echo "Attempting to create $table_name (attempt $((retries+1))/$max_retries)"
        
        # Execute the creation command
        if clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
          --query="$create_query"; then
          
          # Verify creation on all replicas
          if verify_table_creation "$table_name"; then
            echo "Successfully created $table_name on all replicas"
            return 0
          fi
        fi

        retries=$((retries + 1))
        if [ $retries -lt $max_retries ]; then
          echo "Retrying in $retry_delay seconds..."
          sleep $retry_delay
        fi
      done

      echo "ERROR: Failed to create $table_name after $max_retries attempts"
      return 1
    }

    verify_table_creation() {
      local table_name=$1
      local hosts=$(clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
        --query="SELECT host_name FROM system.clusters WHERE cluster = '${CLUSTER_NAME}'")
      
      local missing_hosts=""
      for host in $hosts; do
        local exists=$(clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host $host \
          --query="SELECT count() FROM system.tables WHERE database = 'tokenvisor_emu' AND name = '${table_name}'")
        
        if [ "$exists" -ne "1" ]; then
          missing_hosts+=" $host"
        fi
      done

      if [ -z "$missing_hosts" ]; then
        return 0
      else
        echo "Table $table_name missing on:$missing_hosts"
        return 1
      fi
    }

    # Wait for the cluster to be ready
    echo "Waiting for ClickHouse cluster '${CLUSTER_NAME}' to be ready..."
    RETRIES_READINESS=0
    MAX_RETRIES_READINESS=30
    RETRY_DELAY_READINESS=10

    until clickhouse-client --user "${CH_USER}" --password "${CH_PASSWORD}" --host "${CH_HOST}" \
      --query="SELECT 1" >/dev/null 2>&1; do
      RETRIES_READINESS=$((RETRIES_READINESS+1))
      if [ $RETRIES_READINESS -ge $MAX_RETRIES_READINESS ]; then
        echo "Failed to connect to ClickHouse host ${CH_HOST}. Exiting..."
        exit 1
      fi
      echo "Cannot connect to ${CH_HOST}. Retrying in $RETRY_DELAY_READINESS seconds..."
      sleep $RETRY_DELAY_READINESS
    done

    while true; do
      CLUSTER_COUNT=$(clickhouse-client --user "${CH_USER}" --password "${CH_PASSWORD}" --host "${CH_HOST}" \
        --query="SELECT count() FROM system.clusters WHERE cluster = '${CLUSTER_NAME}'" 2>/dev/null || echo "0")
      case "${CLUSTER_COUNT}" in
        ''|*[!0-9]*)
          CLUSTER_COUNT=0
          ;;
      esac

      if [ "$CLUSTER_COUNT" -eq "$EXPECTED_CLUSTER_COUNT" ]; then
        echo "ClickHouse cluster is ready with $CLUSTER_COUNT nodes."
        break
      fi

      RETRIES_READINESS=$((RETRIES_READINESS+1))
      if [ $RETRIES_READINESS -ge $MAX_RETRIES_READINESS ]; then
        echo "Timed out waiting for ClickHouse cluster. Expected $EXPECTED_CLUSTER_COUNT nodes, found $CLUSTER_COUNT. Exiting..."
        exit 1
      fi
      echo "Waiting for all $EXPECTED_CLUSTER_COUNT nodes to be ready (found $CLUSTER_COUNT)... Retrying in $RETRY_DELAY_READINESS seconds."
      sleep $RETRY_DELAY_READINESS
    done

    echo "Creating a readonly user..."
    echo "Creating read-only user..."
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
      --query="CREATE USER IF NOT EXISTS ${READONLY_USER} IDENTIFIED BY '${READONLY_PASSWORD}' ON CLUSTER '${CLUSTER_NAME}'"

    echo "Granting read-only permissions..."
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
      --query="GRANT SELECT ON *.* TO '${READONLY_USER}' ON CLUSTER '${CLUSTER_NAME}'"

    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
      --query="GRANT REMOTE ON *.* TO '${READONLY_USER}' ON CLUSTER '${CLUSTER_NAME}'"

    echo "Creating databases and tables..."
    echo "Creating llm_usage table..."
    create_table_with_retry "llm_usage" "CREATE TABLE IF NOT EXISTS tokenvisor_emu.llm_usage ON CLUSTER '${CLUSTER_NAME}'
    (
        \`id\` UUID,
        \`org_id\` String,
        \`proj_id\` String,
        \`user_id\` String,
        \`timestamp\` DateTime64(6, 'UTC'),
        \`model\` String,
        \`input_token\` UInt32,
        \`cached_input_token\` UInt32,
        \`output_token\` UInt32,
        \`cost\` Decimal128(12),
        \`input_cost\` Decimal128(12),
        \`cached_input_cost\` Decimal128(12),
        \`output_cost\` Decimal128(12),
        \`pricing_breakdown\` JSON
    )
    ENGINE=ReplicatedMergeTree
    PARTITION BY toYYYYMM(timestamp)
    ORDER BY (org_id, timestamp, model)"

    echo "Creating embed_usage table..."
    create_table_with_retry "embed_usage" "CREATE TABLE IF NOT EXISTS tokenvisor_emu.embed_usage ON CLUSTER '${CLUSTER_NAME}'
    (
        \`id\` UUID,
        \`org_id\` String,
        \`proj_id\` String,
        \`user_id\` String,
        \`timestamp\` DateTime64(6, 'UTC'),
        \`model\` String,
        \`num_token\` UInt32,
        \`cost\` Decimal128(12)
    )
    ENGINE=ReplicatedMergeTree
    PARTITION BY toYYYYMM(timestamp)
    ORDER BY (org_id, timestamp, model)"

    echo "Creating rerank_usage table..."
    create_table_with_retry "rerank_usage" "CREATE TABLE IF NOT EXISTS tokenvisor_emu.rerank_usage ON CLUSTER '${CLUSTER_NAME}'
    (
        \`id\` UUID,
        \`org_id\` String,
        \`proj_id\` String,
        \`user_id\` String,
        \`timestamp\` DateTime64(6, 'UTC'),
        \`model\` String,
        \`num_search\` UInt32,
        \`cost\` Decimal128(12)
    )
    ENGINE=ReplicatedMergeTree
    PARTITION BY toYYYYMM(timestamp)
    ORDER BY (org_id, timestamp, model)"

    echo "Creating guard_usage table..."
    create_table_with_retry "guard_usage" "CREATE TABLE IF NOT EXISTS tokenvisor_emu.guard_usage ON CLUSTER '${CLUSTER_NAME}'
    (
        \`id\` UUID,
        \`org_id\` String,
        \`proj_id\` String,
        \`user_id\` String,
        \`timestamp\` DateTime64(6, 'UTC'),
        \`guard_model_id\` String,
        \`target_model_id\` String,
        \`scope\` String,
        \`policy_id\` String,
        \`mode\` String,
        \`input_token\` UInt32,
        \`output_token\` UInt32,
        \`cost\` Decimal128(12),
        \`input_cost\` Decimal128(12),
        \`output_cost\` Decimal128(12)
    )
    ENGINE=ReplicatedMergeTree
    PARTITION BY toYYYYMM(timestamp)
    ORDER BY (org_id, timestamp, guard_model_id, scope)"

    echo "Creating deployment_usage table..."
    create_table_with_retry "deployment_usage" "CREATE TABLE IF NOT EXISTS tokenvisor_emu.deployment_usage ON CLUSTER '${CLUSTER_NAME}'
    (
        \`id\` UUID,
        \`model_id\` String,
        \`deployment_id\` String,
        \`huggingface_id\` String,
        \`accelerator\` String,
        \`num_accelerator\` UInt32,
        \`timestamp\` DateTime64(6, 'UTC'),
        \`num_replica\` UInt32
    )
    ENGINE=ReplicatedMergeTree
    PARTITION BY toYYYYMM(timestamp)
    ORDER BY (huggingface_id, timestamp)"

    echo "Creating emu_traces table..."
    create_table_with_retry "emu_traces" "CREATE TABLE IF NOT EXISTS tokenvisor_emu.emu_traces ON CLUSTER '${CLUSTER_NAME}'
    (
        \`Timestamp\` DateTime64(9) CODEC(Delta(8), ZSTD(1)),
        \`TraceId\` String CODEC(ZSTD(1)),
        \`SpanId\` String CODEC(ZSTD(1)),
        \`ParentSpanId\` String CODEC(ZSTD(1)),
        \`TraceState\` String CODEC(ZSTD(1)),
        \`SpanName\` LowCardinality(String) CODEC(ZSTD(1)),
        \`SpanKind\` LowCardinality(String) CODEC(ZSTD(1)),
        \`ServiceName\` LowCardinality(String) CODEC(ZSTD(1)),
        \`ResourceAttributes\` Map(LowCardinality(String), String) CODEC(ZSTD(1)),
        \`ScopeName\` String CODEC(ZSTD(1)),
        \`ScopeVersion\` String CODEC(ZSTD(1)),
        \`SpanAttributes\` Map(LowCardinality(String), String) CODEC(ZSTD(1)),
        \`Duration\` Int64 CODEC(ZSTD(1)),
        \`StatusCode\` LowCardinality(String) CODEC(ZSTD(1)),
        \`StatusMessage\` String CODEC(ZSTD(1)),
        \`Events.Timestamp\` Array(DateTime64(9)) CODEC(ZSTD(1)),
        \`Events.Name\` Array(LowCardinality(String)) CODEC(ZSTD(1)),
        \`Events.Attributes\` Array(Map(LowCardinality(String), String)) CODEC(ZSTD(1)),
        \`Links.TraceId\` Array(String) CODEC(ZSTD(1)),
        \`Links.SpanId\` Array(String) CODEC(ZSTD(1)),
        \`Links.TraceState\` Array(String) CODEC(ZSTD(1)),
        \`Links.Attributes\` Array(Map(LowCardinality(String), String)) CODEC(ZSTD(1)),
        INDEX idx_trace_id TraceId TYPE bloom_filter(0.001) GRANULARITY 1,
        INDEX idx_res_attr_key mapKeys(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
        INDEX idx_res_attr_value mapValues(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
        INDEX idx_span_attr_key mapKeys(SpanAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
        INDEX idx_span_attr_value mapValues(SpanAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
        INDEX idx_duration Duration TYPE minmax GRANULARITY 1
    )
    ENGINE = ReplicatedMergeTree
    PARTITION BY toDate(Timestamp)
    ORDER BY (ServiceName, SpanName, toUnixTimestamp(Timestamp), TraceId)
    TTL toDateTime(Timestamp) + toIntervalDay(3)
    SETTINGS index_granularity = 8192, ttl_only_drop_parts = 1"

    echo "Creating emu_traces_trace_id_ts table..."
    create_table_with_retry "emu_traces_trace_id_ts" "CREATE TABLE IF NOT EXISTS tokenvisor_emu.emu_traces_trace_id_ts ON CLUSTER '${CLUSTER_NAME}'
    (
      \`TraceId\` String CODEC(ZSTD(1)),
      \`Start\` DateTime64(9) CODEC(Delta(8), ZSTD(1)),
      \`End\` DateTime64(9) CODEC(Delta(8), ZSTD(1)),
      INDEX idx_trace_id TraceId TYPE bloom_filter(0.01) GRANULARITY 1
    )
    ENGINE = ReplicatedMergeTree
    ORDER BY (TraceId, toUnixTimestamp(Start))
    TTL toDateTime(Start) + toIntervalDay(3)"

    echo "Creating emu_traces_trace_id_ts_mv table..."
    create_table_with_retry "emu_traces_trace_id_ts_mv" "CREATE MATERIALIZED VIEW IF NOT EXISTS tokenvisor_emu.emu_traces_trace_id_ts_mv ON CLUSTER '${CLUSTER_NAME}' TO tokenvisor_emu.emu_traces_trace_id_ts
    (
      \`TraceId\` String,
      \`Start\` DateTime64(9),
      \`End\` DateTime64(9)
    ) AS
    SELECT
      TraceId,
      min(Timestamp) AS Start,
      max(Timestamp) AS End
    FROM tokenvisor_emu.emu_traces
    WHERE TraceId != ''
    GROUP BY TraceId"

    echo "2025-09-05: Migrating costs related column to Decimal128(12)"
    echo "2026-05-14: Add cached and breakdown column to llm_usage"
    echo "Migrating llm_usage"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.llm_usage ON CLUSTER '${CLUSTER_NAME}' MODIFY COLUMN cost Decimal128(12)"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.llm_usage ON CLUSTER '${CLUSTER_NAME}' MODIFY COLUMN input_cost Decimal128(12)"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.llm_usage ON CLUSTER '${CLUSTER_NAME}' MODIFY COLUMN output_cost Decimal128(12)"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.llm_usage ON CLUSTER '${CLUSTER_NAME}' ADD COLUMN IF NOT EXISTS cached_input_token UInt32"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.llm_usage ON CLUSTER '${CLUSTER_NAME}' ADD COLUMN IF NOT EXISTS cached_input_cost Decimal128(12)"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.llm_usage ON CLUSTER '${CLUSTER_NAME}' ADD COLUMN IF NOT EXISTS pricing_breakdown JSON"

    echo "Migrating embed_usage"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.embed_usage ON CLUSTER '${CLUSTER_NAME}' MODIFY COLUMN cost Decimal128(12)"

    echo "Migrating rerank_usage"
    clickhouse-client --user ${CH_USER} --password ${CH_PASSWORD} --host ${CH_HOST} \
    --query="ALTER TABLE tokenvisor_emu.rerank_usage ON CLUSTER '${CLUSTER_NAME}' MODIFY COLUMN cost Decimal128(12)"
---
apiVersion: batch/v1
kind: Job
metadata:
  name: clickhouse-init-db
  annotations:
    linkerd.io/inject: disabled # Disable Linkerd injection
spec:
  template:
    metadata:
      labels:
        app: init-clickhouse-db
      annotations:
        linkerd.io/inject: disabled # Disable Linkerd injection
    spec:
      containers:
        - name: clickhouse-init-db
          image: clickhouse/clickhouse-client:latest
          command:
            - "/bin/bash"
            - "-c"
            - |
              cp /usr/src/app/01_create_databases.sh /tmp/01_create_databases.sh
              chmod +x /tmp/01_create_databases.sh
              /tmp/01_create_databases.sh
          envFrom:
            - configMapRef:
                name: clickhouse-env-config
          env:
            - name: CH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: clickhouse-secret
                  key: db_user_password
            - name: READONLY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: clickhouse-secret
                  key: db_readonly_user_password
          volumeMounts:
            - mountPath: /usr/src/app
              name: clickhouse-init-db
      volumes:
        - name: clickhouse-init-db
          configMap:
            name: init-clickhouse-db
      restartPolicy: Never
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: clickhouse-env-config
data:
  CH_USER: emuuser
  CH_HOST: clickhouse-tokenvisor.tokenvisor.svc.cluster.local
  CLUSTER_NAME: tv-cluster
  EXPECTED_CLUSTER_NUMBER: "3"
  READONLY_USER: emu_readonly
